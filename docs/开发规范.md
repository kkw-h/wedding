# 开发规范

- TypeScript 严格模式，所有数据模型与接口返回需定义类型。
- 组件分层：页面在 `pages`，通用组件在 `shared/components`，业务服务在 `services`。
- 路由按模块划分，与页面设计文档路径一致。
- 权限在页面层隐藏按钮；预算模块只读。
- 样式使用 CSS Modules + Sass，避免全局污染。
- Markdown 渲染遵循 CommonMark，禁止 HTML 标签。
- 电话字段存储原始字符串，展示打码并支持拨号链接。
- 统一使用 AntD Form/Table；分页/筛选使用共享 hooks。